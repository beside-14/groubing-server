spring:
  config:
    activate:
      on-profile: local
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
    defer-datasource-initialization: true
  h2:
    console:
      enabled: false
      path: /console
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:groubing
    username: sa
    password:
